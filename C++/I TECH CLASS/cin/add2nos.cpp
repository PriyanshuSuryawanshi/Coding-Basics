#include <iostream>
using namespace std;

int main() {
    int x, y;
    int sum;
    cout << "Type number 1: ";
    cin >> x;
    cout << "Type number 2: ";
    cin >> y;
    // sum = x + y;
    cout << "Sum is: " << x+y;
    cout << "\nSubtraction is: " << x-y;
    cout << "\nMultiplication is: " << x*y;
    cout << "\nDivision is: " << x/y;
    cout << "\nSquare of number 1 is : " << x*x << "\nSquare of number 2 is : " << y*y ;
return 0;
}
